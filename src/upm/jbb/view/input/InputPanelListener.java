package upm.jbb.view.input;
/**
 * Se invoca este método cuando el usuario a dado una respuesta y pulsa enter o ha seleccionado de una lista
 * Se crea para avisar al contenedor del Panel, normalmente un JDialoj
 * 
 * @author jbernal
 *
 */
public interface InputPanelListener {
	public void accept();
}
